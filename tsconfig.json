{
  /* Common TS compiler settings for compilation outputs, IDEs (like VSCode) and test runner (Karma+Mocha) */
  "compilerOptions": {
    "strict": true,
    "target": "ESNext",
    "importHelpers": true,
    "moduleResolution": "Node"
  }
}